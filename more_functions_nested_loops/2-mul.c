#include "main.h"

/**
 * mul - Multiples two integers
 * @x: The first number to multiply
 * @y: The second number to multyply with first number
 * Return: El resultado de la multiplicacion
 */

int mul(int x, int y)
{
	return (x * y);
}
