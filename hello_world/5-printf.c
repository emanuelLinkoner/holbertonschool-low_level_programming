#include <stdio.h>
/**
 * main - Print "with proper grammar, but the outcome is a pieceof art,"
 * Return: 0. (Success)
 */
int main(void)
{
 printf("with proper grammar, but the outcome is a pieceof art,\n");
return (0);
}

